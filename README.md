# Передержка домашних животных

Консольное приложение для гостиницы передержки домашних животных.

## Назначение
Приложение передержки домашних животных. Оно помогает
оформлять бронирования, проверять, можно ли принять питомца, подбирать ему
подходящее место и рассчитывать стоимость проживания.

## Целевая аудитория
1. Владельцы кошек и собак, которым нужно оставить питомца на время отъезда
2. Администратор гостиницы, который принимает заявки и управляет бронированиями

## Предметная область
Передержка домашних животных. Владелец временно оставляет питомца в гостинице
на определённые даты. Гостиница принимает животных (учитываются вид,
возраст и наличие прививок), размещает питомца в месте подходящего размера
и берёт плату за каждые сутки проживания

## Основные сущности
1. Пользователь. Участник системы (имя, контакты, роль), от роли зависят права
   1. клиент (владелец питомца) оформляет бронирования для своих питомцев;
   2. администратор (сотрудник гостиницы) управляет местами, видит все заявки,
      подтверждает и отменяет любые бронирования
2. Питомец. Животное клиента (вид, возраст, вес, наличие прививок)
3. Место. Вольер или комната определённого размера (малое, среднее, большое)
4. Бронирование. Заявка на проживание питомца в конкретном месте на даты
   заезда и выезда

## Основные функции
1. Проверка допуска питомца (гостиница принимает кошек и собак
   от 3 месяцев с прививками)
2. Подбор места по весу питомца (малое, среднее или большое)
3. Расчёт стоимости передержки. Тариф за сутки, который зависит от размера
   места, число суток по датам заезда и выезда
4. Просмотр и отмена бронирований. Клиент работает со своими,
   администратор со всеми

## Возможности приложения
1. просмотр мест, владельцев и питомцев;
2. добавление владельца и питомца;
3. поиск питомца по кличке или имени владельца;
4. проверка допуска питомца и подбор места по весу;
5. проверка занятости места на выбранные даты;
6. оформление бронирования с расчётом стоимости;
7. отмена бронирования с сохранением его в истории;
8. статистика по бронированиям, питомцам и местам

## Классы

### User
Пользователь системы: владелец питомца или администратор.

| Атрибут | Описание |
|---|---|
| `id` | идентификатор |
| `name` | имя и фамилия |
| `phone` | телефон |
| `role` | роль: клиент или администратор |

| Метод | Назначение |
|---|---|
| `__str__()` | строковое представление, роль видна только у администратора |
| `from_data()` | `@classmethod`, создаёт объект из данных JSON |
| `to_data()` | возвращает данные для записи в JSON |

### Pet
Питомец, которого владелец оставляет в гостинице.

| Атрибут | Описание |
|---|---|
| `id` | идентификатор |
| `name` | кличка |
| `species` | вид |
| `age_months` | возраст в месяцах |
| `weight_kg` | вес в килограммах |
| `is_vaccinated` | наличие прививок |
| `owner` | объект `User`, владелец питомца |

| Метод | Назначение |
|---|---|
| `is_supported_species()` | `@staticmethod`, проверяет, принимает ли гостиница животных этого вида |
| `rejection_reason()` | возвращает причину отказа в приёме или пустую строку |
| `can_be_accepted()` | проверяет, может ли гостиница принять питомца |
| `required_room_size()` | подбирает размер места по весу |
| `__str__()` | строковое представление с именем владельца |
| `from_data()` | `@classmethod`, создаёт объект из данных JSON и объекта владельца |
| `to_data()` | возвращает данные для JSON, владелец записывается как `owner_id` |

### Room
Место в гостинице: вольер или комната определённого размера.

| Атрибут | Описание |
|---|---|
| `id` | идентификатор |
| `name` | название |
| `size` | размер: малое, среднее или большое |
| `price_per_night` | тариф за сутки в рублях |

| Метод | Назначение |
|---|---|
| `is_suitable_for(pet)` | проверяет, подходит ли место питомцу по размеру |
| `cost_for(check_in, check_out)` | считает стоимость проживания за период |
| `__str__()` | строковое представление с размером и тарифом |
| `from_data()` | `@classmethod`, создаёт объект из данных JSON |
| `to_data()` | возвращает данные для записи в JSON |

### Booking
Бронирование места для питомца на период проживания.

| Атрибут | Описание |
|---|---|
| `id` | идентификатор |
| `pet` | объект `Pet` |
| `room` | объект `Room` |
| `check_in` | дата заезда |
| `check_out` | дата выезда |
| `is_cancelled` | признак отмены, только для чтения |

| Метод или свойство | Назначение |
|---|---|
| `cancel()` | отменяет бронирование, единственный способ изменить признак отмены |
| `overlaps(check_in, check_out)` | проверяет пересечение с другим периодом |
| `status` | `@property`, возвращает «активно» или «отменено» |
| `nights` | `@property`, число суток проживания |
| `cost` | `@property`, стоимость по тарифу своего места |
| `__str__()` | строковое представление с состоянием бронирования |
| `from_data()` | `@classmethod`, создаёт объект из JSON и объектов питомца и места |
| `to_data()` | возвращает данные для JSON, питомец и место записываются как идентификаторы |

### Исключения
Ошибки оформления бронирования образуют иерархию. Любую из них можно
перехватить через общий класс `BookingError`.

```
BookingError
├── InvalidPeriodError      дата выезда не позже даты заезда
├── PetNotAcceptedError     питомец не проходит условия приёма
├── RoomNotSuitableError    место не подходит питомцу по размеру
└── RoomNotAvailableError   место занято на эти даты
```

## Взаимодействие объектов
```
Booking
├── pet  ──→ Pet
│            └── owner ──→ User
└── room ──→ Room
```

1. Питомец хранит объект своего владельца, а не его имя
2. Бронирование хранит объекты питомца и места, поэтому через него доступны
   их данные: `booking.room.name`, `booking.pet.owner.phone`
3. Место проверяет, подходит ли ему питомец: `room.is_suitable_for(pet)`
   обращается к методу питомца `required_room_size()`
4. Бронирование не считает стоимость само, а спрашивает её у своего места
   через `room.cost_for()`
5. Отменённое бронирование остаётся в списке и не блокирует место

## Функции над коллекциями
Операции, которые относятся к одному объекту, реализованы методами классов.
Операции над списками объектов остались функциями модулей:

1. поиск по идентификатору и по части имени;
2. сортировка и подсчёт по видам и размерам;
3. проверка доступности места среди всех бронирований;
4. создание и отмена бронирований;
5. статистика;
6. загрузка и сохранение данных;
7. ввод данных с проверкой

## Хранение данных
Данные хранятся в каталоге `data` в файлах JSON. В файлах записаны только
данные и идентификаторы связанных объектов, а приложение работает
с объектами.

При запуске `storage.py` создаёт объекты в таком порядке: пользователи,
места, питомцы со ссылкой на владельца, бронирования со ссылками на питомца
и место. Если связанный объект не найден, запись пропускается с сообщением.
При сохранении ссылки на объекты превращаются обратно в идентификаторы.
Если файл отсутствует или повреждён, программа стартует с пустыми данными.

Пользователь:

```json
{
    "id": 1,
    "name": "Иванова Анна",
    "phone": "+7 900 111 22 33",
    "role": "клиент"
}
```

Место:

```json
{
    "id": 1,
    "name": "Вольер 1",
    "size": "малое",
    "price_per_night": 800
}
```

Питомец:

```json
{
    "id": 1,
    "name": "Барсик",
    "species": "кошка",
    "age_months": 18,
    "weight_kg": 4.5,
    "is_vaccinated": true,
    "owner_id": 1
}
```

Бронирование:

```json
{
    "id": 1,
    "pet_id": 1,
    "room_id": 1,
    "check_in": "2026-10-01",
    "check_out": "2026-10-08",
    "is_cancelled": false
}
```

## Структура проекта
```
pet-boarding/
├── main.py                     точка запуска
├── requirements.txt            зависимости разработки
├── setup.cfg                   настройки pytest и flake8
├── src/
│   ├── __init__.py
│   ├── models/                 классы предметной области
│   │   ├── __init__.py         экспорт классов
│   │   ├── common.py           общие функции коллекций
│   │   ├── users.py            User и функции списка пользователей
│   │   ├── pets.py             Pet и функции списка питомцев
│   │   ├── rooms.py            Room и функции списка мест
│   │   └── bookings.py         Booking и функции списка бронирований
│   ├── cli.py                  меню и вывод данных
│   ├── constants.py            константы проекта
│   ├── exceptions.py           исключения предметной области
│   ├── storage.py              преобразование JSON в объекты и обратно
│   └── utils.py                ввод данных с проверкой
├── data/                       файлы данных
│   ├── users.json
│   ├── pets.json
│   ├── rooms.json
│   └── bookings.json
└── tests/                      автоматизированные тесты
    ├── conftest.py             общие объекты для тестов
    ├── test_users.py
    ├── test_pets.py
    ├── test_rooms.py
    └── test_bookings.py
```

## Требования
1. Python 3.9 или новее;
2. pytest;
3. flake8

## Установка
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Запуск программы
```bash
python main.py
```

## Запуск тестов
```bash
pytest -v
```

Тесты проверяют:

1. создание объектов и их атрибуты;
2. строковое представление и преобразование в JSON и обратно;
3. связи между объектами;
4. правила приёма питомцев и подбора мест;
5. правило доступности: второе активное бронирование места на те же даты
   запрещено, отменённое бронирование место не блокирует;
6. исключения при оформлении бронирования;
7. статистику

## Проверка качества кода
```bash
flake8
```

## План развития
На следующих этапах я планирую:

1. разработку веб-приложения;
2. подключение базы данных;
3. реализацию пользователей;
4. разработку API;
5. контейнеризацию приложения
